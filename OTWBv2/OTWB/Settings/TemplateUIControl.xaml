<UserControl
    x:Class="OTWB.Settings.TemplateUIControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OTWB.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="400" Height="165">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="7*" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="TemplateName" Text="{Binding Name}" VerticalAlignment="Top" Height="55"/>
        <TextBlock x:Name="Value" Text="{Binding Template, Mode=TwoWay}" Grid.Column="1" VerticalAlignment="Top" Height="165" />
    </Grid>
</UserControl>
